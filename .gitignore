# macOS
.DS_Store
._*

# Editor
.vscode/
.idea/
*.swp
*~

# Local backups (if user accidentally drops them in repo)
*.backup
*.bak

# Pele's own runtime artifacts (none planned, but reserve)
.pele-cache/

# Skill evals — local only, may contain project-internal names / paths
core/skills/*/evals/

# maintainer-only sync tool — kept local, contains real names
scripts/sync-from-local.sh
.sync-port-state
