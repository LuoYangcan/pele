{
  "_comment": "Pele does not auto-merge permissions. Start with no broad allow rules; add only project-specific Bash, MCP, or Read patterns you trust.",
  "_notes": [
    "The iOS UI workflow uses the sim-use CLI plus xcrun simctl, not ios-simulator MCP.",
    "Avoid a blanket Read allow unless you also deny sensitive paths such as ~/.ssh/**, ~/.aws/**, and **/.env*."
  ],
  "permissions": {
    "allow": []
  }
}
