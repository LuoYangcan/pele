# Commit Message 风格

- 使用 conventional commits：`type(scope): description`
- **单行、简短**，只写"做了什么"
- **不要**加 `Co-Authored-By:`、`🤖 Generated with Claude Code` 等尾巴
- 原因、背景、动机留给 PR 描述或代码注释，不进 commit message
- 用 `git commit -m "..."` 一行搞定；不要用 HEREDOC 堆多行 body
